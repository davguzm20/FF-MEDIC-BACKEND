# PostgreSQL (Neon)
DATABASE_URL=example-postgresql-url

# JWT
JWT_SECRET=example-jwt-secret
JWT_REFRESH_SECRET=example-jwt-refresh-secret
JWT_EXPIRES_IN=example-jwt-expires-in
JWT_REFRESH_EXPIRES_IN=example-jwt-refresh-expires-in

# Server
PORT=example-port
CORS_ORIGINS=example-cors-origins
BCRYPT_SALT_ROUNDS=example-bcrypt-salt-rounds
SWAGGER_ENABLED=example-swagger-enabled

# Redis (Upstash)
REDIS_URL=example-redis-url
REDIS_BLACKLIST_TTL=example-redis-blacklist-ttl
RESET_TOKEN_TTL=example-reset-token-ttl

# SendGrid
SENDGRID_API_KEY=example-sendgrid-api-key
MAIL_FROM=example-mail-from